eureka:
  instance:
    hostname: client
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/,http://localhost:8762/eureka/,http://localhost:8763/eureka/
spring:
  application:
    name: eureka-client1
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/hello?useSSL=false&characterEncoding=utf-8
    username: root
    password: sen515216
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8081
